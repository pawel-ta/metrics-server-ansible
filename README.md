# metrics-server-ansible
This is an Ansible role for configuring Prometheus / Node Exporter/ Grafana setup on a single or multiple machines.

The role requires Docker to be installed on target hosts.
